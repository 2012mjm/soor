import {createAction} from 'redux-act'

export const setAttributeList = createAction('SET_ATTRIBUTE_LIST', data => data)
export const setCurrentAttribute = createAction('SET_CURRENT_ATTRIBUTE', data => data)
